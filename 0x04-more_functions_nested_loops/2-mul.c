#include "main.h"
/**
 * mul - Multiply two integers .
 * @a: the first integer (int type).
 * @b: the second integer (int type).
 * Return: the result of multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
